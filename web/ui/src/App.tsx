// The application shell. The §13.10 surfaces (domain browser, filtered
// search, artifact viewer, and layer panel) are built against the design
// pass's output in web/design/, with web/DESIGN.md as the brief.
export function App() {
  return (
    <main className="app-shell">
      <h1>Podium</h1>
    </main>
  );
}

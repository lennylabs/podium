.DS_Store
tmp/
bin/
coverage.out
coverage.html
*.test
*.out
/podium
/podium-server
/podium-mcp
__pycache__/
*.pyc
.venv/
*.egg-info/
build/
# The site generator's build stage is source, not output.
!site/src/build/
dist/
# Generated by setuptools-scm at build/install time.
sdks/podium-py/podium/_version.py
node_modules/
# Vitest coverage output and the scratch corpora its tests write.
site/coverage/
site/test/.tmp/
.env.local
.envrc.local

# Local integration/live-test credentials. Copy test.env.example to test.env
# and fill it in; the test harness loads it (see internal/testenv + Makefile).
/test.env

.claude/scheduled_tasks.lock

# Local archives. One was committed by accident; the release ships built
# artifacts from the workflow rather than anything checked in here.
*.zip
